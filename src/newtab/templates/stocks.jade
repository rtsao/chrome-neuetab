h2.fadeIn(ng-show="app.stocks.length") Stocks
div.stockList
  div.stock(ng-repeat="stock in app.stocks track by $index")
    div.name {{stock.name}}
      span.symbol {{stock.e}} : {{stock.t}}
    div.price {{stock.l}}
      span.change(class="{{stock.ccol}}") {{stock.c}} ({{stock.cp}}%)
    div.time {{moment(stock.lt_dts).fromNow();}}